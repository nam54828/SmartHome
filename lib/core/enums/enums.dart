/// =================== Base Enum ===================//

enum TypeOfDevice {
  bigDevice,
  smallDevice,
  normalDevice,
  mediumSmallDevice,
}

enum CustomButtonType {
  DEFAULT,
  OUTLINE,
}

enum CustomImageType {
  SVG,
  IMAGE,
  NOT_IMAGE,
}

enum CustomImageUrlType {
  NETWORK,
  ASSET,
  FILE,
  ICON,
  IMAGE_CIRCLE,
}

enum CustomInputType {
  TEXT,
  PASSWORD,
  NUMBER,
  DOUBLE,
  PRICE,
  EMAIL,
  PHONE,
  INCREMENT,
  MULTILINE,
  DATE,
}

enum CustomPickerDate {
  MATERIAL,
  CUPERTINO,
}

enum TypeOfAlert {
  ERROR,
  SUCCESS,
  INFO,
  WARRING,
}

enum TypeOfBannerAds {
  BANNER,
  COLLAPSIBLE_BANNER_TOP,
  COLLAPSIBLE_BANNER_BOTTOM,
}

enum StatusLoadAds {
  LOAD,
  SUCCESS,
  FAILED,
}

/// =================== Base Enum ===================//

